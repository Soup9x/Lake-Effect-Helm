/**
 * Tailwind CSS v4 has no JavaScript config file: theme tokens are declared in
 * CSS with @theme, which is why there is no tailwind.config.ts here. See
 * src/app/globals.css.
 */
const config = {
  plugins: { '@tailwindcss/postcss': {} },
};

export default config;
